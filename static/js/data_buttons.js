
function editRow(doc_id) {
    // Redirect to edit page with document id
    alert("document id is" + doc_id + "\nEdit Feature is not Available ");
    }

function removeRow(doc_id) {
    // Confirm deletion and redirect to delete route with document id
    alert("document id is" + doc_id + "\nRemove Feature is not Available.");
    // db.for_text.delete_one({'_id': ObjectId(doc_id)});
    }


